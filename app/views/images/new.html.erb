<h2 class="gvc-title">画像を選択してください</h2>
<%= form_for @image, url: upload_images_path, html: { multipart: true } do |f| %>
  <p class="select-image-input"><%= f.file_field :file %></p>
  <p class="select-image-submit"><%= f.submit 'アップロード'%></p>
<% end %>
